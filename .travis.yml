language: python
python:
  - "3.6"
script:
  - ./test
install:
  - pip install -r requirements.txt
matrix:
  include:
    - name: "Linux"
      os: linux
    - name: "OSX"
      os: osx
      language: shell # 'language: python' is an error on Travis CI macOS
